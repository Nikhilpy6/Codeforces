t = int(input())
while t:
    n, x = map(int, input().split())
    a = list(map(int, input().split()))
    last = 0
    m = 0
    for i in range(n):
        m = max(m, a[i] - last)
        last = a[i]

    m = max(m, 2 * (x - last))

    print(m)
